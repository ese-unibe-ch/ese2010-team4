package models.annotations;

public @interface ForTestingOnly {
	/**
	 * This annotation shows that a method is only public because it's needed
	 * for tests it would better be a private method instead!
	 */
}
